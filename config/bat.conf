# bat --generate-config-file
--theme=base16
# Explicit + separated component list rather than --style=-numbers, which older
# bat versions (e.g. Ubuntu 20.04's 0.12.1) do not support.
--style=header,grid
